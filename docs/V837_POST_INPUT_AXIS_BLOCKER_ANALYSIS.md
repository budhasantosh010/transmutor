# V837 Post-Input-Axis Blocker Analysis

## Closed evidence

The current best neutral substrate remains V837y Y3 at 3/5. V837z showed that forcing a fully synchronous candidate stage is harmful. V837aa showed that candidate laws are genuinely heterogeneous and do not collapse to one common signed/permuted law or a stable 2–5-type vocabulary. V837ab showed that the T2 input projection is exactly foldable as a function but that trainable factorization on either one active reference input pathway can preserve 4/5. V837ac then transferred the smallest supported reference property—controller-input factorization—to Y3 and remained at 3/5.

Therefore the input organization axis is closed for the present neutral substrate.

## Remaining exact difference selected for later work

The next single variable is:

`CANDIDATE_STATE_GEOMETRY_LOCALIZATION`

Specifically:

> one dense 13D candidate/state coordinate geometry versus the partitioned 10×4 local candidate/state geometry, while preserving the already-established useful mechanisms and avoiding a repeat of V837q shared-state-only testing.

This is not implemented in the current program.

## Why this is not simply shared state again

V837q already localized state ownership/sharing and did not establish that generic state sharing alone fixes representation adequacy. A future geometry experiment must isolate the coordinate geometry of candidate/state computation itself rather than merely replacing local state with a shared state tensor.

It must hold fixed, as far as the test design permits:

- useful global temporal control,
- useful cross-cell candidate integration,
- historical within-step cascade,
- data regime and optimizer,
- input axis closure established here,
- frozen representation gate.

## Locked state

- representation adequacy: FAIL
- sample-efficiency retest: BLOCKED
- structural search: BLOCKED
- primitive mining: BLOCKED
- fresh audit consumed: 0
- primitives promoted: 0
- large persistent storage: NOT TESTED
- V838: NOT STARTED

No dense-state or geometry experiment is authorized or implemented by this report.
