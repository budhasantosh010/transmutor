# V837ac — Authorized Controller-Input Transfer

V837ab localized T2's input-factorization benefit to either active path independently and machine-selected the smaller intervention: `TRAINABLE_CONTROLLER_INPUT_FACTORIZATION`. V837ac therefore keeps the exact Y3 neutral substrate and changes only the global scalar controller's input parameterization.

AC1 factorizes the historical Y3 controller input through a trainable 6→6 projection while preserving the exact AC0 effective controller input function at step 0. AC1F is the projection-free folded control. No candidate projection or de-shared control is meaningful in this controller-only branch.
