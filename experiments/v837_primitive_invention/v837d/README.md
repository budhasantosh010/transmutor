# V837d Representation Recovery

Status: implementation / diagnostic phase.

V837d changes one principal representation feature: raw observation accessibility. It compares historical broadcast access against deterministic fixed-sparse access in the same 10-cell/55-edge high-capacity neutral graph, with identical tasks, ablation data, full-AdamW budget and cell law.

The historical broadcast rerun must first match the preserved blocker diagnostic closely enough to trust the refactor. Sparse conditions are not interpreted if baseline drift exceeds the frozen tolerance.

Primitive mining remains blocked and fresh-audit seeds remain unused.
