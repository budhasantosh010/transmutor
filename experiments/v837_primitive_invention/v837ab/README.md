# V837ab — Input Factorization Necessity

V837ab asks whether the successful V837t T2 reference actually needs its learned shared 6→6 input projection during optimization. Because every active downstream input path is linear before the next nonlinearity, the projection is exactly foldable into the GRU input matrices. AB0–AB4 therefore begin from the same effective input function; only the factorization retained during optimization differs. AB5 is a deliberately unmatched ordinary direct-initialization control.

No fresh-audit seeds, structural search, primitive mining, V838 work, or neutral transfer is permitted until the machine decision authorizes an exact V837ac mode.
