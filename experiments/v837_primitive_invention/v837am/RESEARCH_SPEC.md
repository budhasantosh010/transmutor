# V837am Research Specification

## Frozen question
Why can the causally important recurring V837ak computation not be reused across independently trained organisms?

Hypotheses are predeclared as: H1 context-conditioned interface, H2 primitive boundary too narrow, H3 recurrence is not interchangeable operator equivalence, and conditional H4 temporal context/history is part of the reusable object.

## Search spaces
- AM-A: exactly 126 configurations = 6 static full-affine bundle anchors + 5 context sets × 4 analytic conditioned families × 6 port bundles.
- AM-B: exactly 20 = 10 frozen spatial boundary definitions × {IDENTITY, FULL_AFFINE_ALL_PORTS}.
- AM-C: exactly 7 = H2/H4/H8 temporal models + COMBINED +1/+2/+4 context interactions + whole-system interaction diagnostic.

All selection uses development partitions only. `20000–20127` is inaccessible until `raw/selected_final_hypothesis.json` is frozen and hashed. The fresh audit `90000–90499` is never consumed.

## Permanent failure memory
`FAILURE_ANALYSIS.md`, machine failure ledgers and `docs/V837_FAILURE_LEDGER.md` are append-only scientific memory. Negative experiments shrink future search space and are never silently discarded.
