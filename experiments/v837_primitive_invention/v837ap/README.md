# V837ap

`V837ap_GLOBAL_COORDINATE_OR_NONLINEAR_CAUSAL_STATE` tests whether the causal steering directions established by V837an and shown by V837ao not to be affine global K1 coordinates can be upgraded into compact, causally closed semantic state through a frozen nonlinear/projected geometry ladder.

The program starts at commit `c47649227baa0800d311ff41128d8f169ddf4f0d` and preserves all predecessor source organisms and scientific artifacts. Semantic dimension remains one. Physical carrier dimensions are restricted to `k ∈ {1,2,4,8}` with predeclared scalar, projected, tangent-field, and final-resort phase-atlas families.

The core acceptance sequence is reader → absolute semantic SET + matched controls → quotient sufficiency → natural/interventional commutativity → META confirmation → discovery-final confirmation → geometry freeze → held-out organism compilation/evaluation → cross-organism semantic agreement. Historical validation is reused only after freeze and cannot rescue a failed family.

Run preserved stages through:

```text
python scripts/reproduce_v837_recovery.py --variant v837ap --stage <stage> --execute
```

Available stages are `source`, `baseline`, `k1-charts`, `projected-charts`, `tangent`, `atlas`, `setpoints`, `quotient`, `dynamics`, `meta`, `final`, `freeze`, `heldout`, `agreement`, `robustness`, `law-audit`, and `analyze`.

Hard locks throughout V837ap: no source-model retraining, no source optimizer steps, no architecture changes, no fresh-audit use, no cross-organism physical-state/q alignment, no PrimitiveArchive population, no primitive promotion, and no V838 work.

See `RESEARCH_SPEC.md` and `frozen_global_or_nonlinear_causal_state_gate.json` for the complete human-readable and executable contracts.
