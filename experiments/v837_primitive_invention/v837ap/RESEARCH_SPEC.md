# V837ap — Global Coordinate or Nonlinear Causal State

Program: `V837ap_GLOBAL_COORDINATE_OR_NONLINEAR_CAUSAL_STATE`  
Required START SHA: `c47649227baa0800d311ff41128d8f169ddf4f0d`  
Required research branch: `research/v837-global-coordinate-or-nonlinear-causal-state`

## Mission

V837ao established `CAUSAL_DIRECTION_NOT_GLOBAL_COORDINATE`: V837an's `STATE40-K1` directions are genuine causal steering directions, but the frozen affine/global K1 coordinate family did not establish a stable absolute semantic state. V837ap asks for the smallest predeclared geometry that can upgrade that steering result into a causally closed semantic state without retraining or modifying any source AF1D organism.

The semantic state remains one-dimensional. Only the physical carrier/chart geometry may become nonlinear, state-dependent, projected, or phase-conditional.

## Permanent locks

- Preserve the V837ao diagnosis exactly and preserve the V837an causal-steering result.
- Source AF1D organisms and checkpoints are immutable.
- New source-model fits: 0.
- Source optimizer steps: 0.
- Source training examples: 0.
- Source architecture changes: 0.
- Fresh audit `90000..90499`: unused.
- PrimitiveArchive population: forbidden.
- Primitive promotions: 0.
- V838: not started.
- No cross-organism STATE40 alignment or q-vector alignment.
- No MLP, GP/kernel, arbitrary high-degree polynomial, structure-search, motif-search, routing-search, or rank-4-bus reopening.

The executable machine contract is `frozen_global_or_nonlinear_causal_state_gate.json`. If prose and executable artifacts ever differ, the frozen gate and START-commit source evidence control.

## Powered families and folds

Powered predecessor families:

1. `conditional_routing`
2. `delayed_recall`
3. `iterative_state`
4. `variable_composition`

`partial_observation` remains unresolved/underpowered and is not upgraded by this program.

Reuse the exact V837ao engine-stratified organism folds. Discovery/heldout counts are respectively 5/4, 6/4, 6/4, and 4/4. No new split and no performance sorting are permitted.

## Episode roles

- `AP_CHART_FIT`: `10000..10063`
- `AP_CHART_SELECT`: `10064..10127`
- `AP_WRITER_FIT`: `10128..10191`
- `AP_WRITER_SELECT`: `10192..10255`
- `AP_QUOTIENT`: `10256..10319`
- `AP_DYNAMICS`: `10320..10383`
- `AP_META_CONFIRM`: `10384..10447`
- `AP_DISCOVERY_FINAL`: `10448..10511`
- `REUSED_HISTORICAL_VALIDATION`: `20000..20127`, descriptive/post-freeze only
- `FRESH_AUDIT`: `90000..90499`, unused

Within-program development roles are disjoint. Historical validation is explicitly reused evidence, not a fresh audit.

## AP0–AP2 — source lock, negative anchor, projected spaces

Before nonlinear science:

- verify predecessor hashes and authorization;
- reproduce the frozen V837ao B0 negative anchor to numeric tolerance `1e-9`;
- freeze the inherited organism folds and data-role lock;
- reconstruct nested counterfactual-difference SVD carrier spaces `Q_k` for `k ∈ {1,2,4,8}` using only permitted fit data;
- verify subspace nesting to tolerance `1e-8`.

No historical per-organism V837an/V837ao canonical backend is loaded as a heldout shortcut.

## AP3–AP7 — reader and absolute SET ladder

The frozen complexity order is:

1. K1 affine anchor
2. K1 simple nonlinear charts
3. K1 nonlinear reader plus state-dependent tangent writer
4. K2 projected chart
5. K4 projected chart
6. K8 projected chart
7. frozen phase atlas

K1 continuous chart families: affine, polynomial-2, polynomial-3, monotone PWL-4, monotone PWL-8, monotone PCHIP-6. Binary K1 families: affine, monotone logistic, isotonic-4, isotonic-8. Projected K2/K4/K8 families: linear, quadratic, cubic. Maximum polynomial degree is three and ridge lambda is `1e-6`.

A global reader must satisfy the reader gate on pooled selection data and on every frozen phase. Continuous gates: normalized RMSE ≤ .10, |Pearson| ≥ .95, Spearman ≥ .95. Binary gates: accuracy ≥ .95, balanced accuracy ≥ .95, normalized RMSE ≤ .15. Chart coverage must be at least .95 where applicable.

Continuous targets are frozen from `AP_CHART_FIT` at Q10/Q25/Q40/Q50/Q60/Q75/Q90 with semantic range `Rz = Q95-Q05`. Binary targets are exactly `{-1,+1}`. Small interventions with `|target-current| < .10 Rz` are skipped.

Projected Newton/tangent steps use at most four iterations, gradient epsilon `1e-8`, and a per-step trust radius `2*D90`, where D90 is the FIT-only P90 projected natural counterfactual displacement. A zero semantic delta must be an identity operation.

SET acceptance requires read-after-set NRMSE ≤ .05 continuous or ≤ .10 binary, median counterfactual recovery ≥ .70, direction agreement ≥ .80, task success ≥ .75, trajectory NRMSE ≤ .15, median OOD ratio ≤ 2, and target-magnitude generalization. Specificity additionally requires 32/32 valid Haar random-subspace controls, 32 norm-matched random setters, a valid within-phase shuffled-semantic equal-capacity control, margins ≥ .20, and paired one-sided permutation `p ≤ .01`.

Phase-atlas use is last resort only. The atlas must use the same predeclared chart family/degree across frozen phases; there is no phase-specific degree search.

## AP8–AP10 — quotient and dynamics closure

Quotient pairs are same organism, family, and phase. Pairing uses near-equal semantic readouts (`|Ea-Eb| ≤ .10 Rz`) and maximizes projected private-state separation without using outcomes. Semantics are aligned before applying a common SET and common future context.

Quotient gates: median residual sensitivity ≤ .20, p90 ≤ .40, canonical trajectory disagreement ≤ .10, normalized final-prediction disagreement ≤ .10, task-success disagreement ≤ .10, and median OOD ratio ≤ 2.

Natural and interventional commutativity test closure under the known benchmark transition laws. Natural one-step NRMSE must be ≤ .10. Interventional multi-step trajectory NRMSE must be ≤ .15, final abstract-output recovery ≥ .80, task success ≥ .75, control margin ≥ .20, and OOD ≤ 2. Rollout horizons are 1, 2, 4, and 8, capped by remaining sequence length.

The first geometry in the frozen complexity hierarchy satisfying the complete discovery gate wins; do not score-maximize. Discovery family acceptance requires at least 60% of discovery organisms and representation from both V837aj structural-search engines.

## AP11–AP13 — META, discovery-final, freeze

META uses `AP_META_CONFIRM` with no refit and no geometry fallback. A META failure makes that family null. `AP_DISCOVERY_FINAL` is another no-refit confirmation. After those decisions, freeze one family geometry or null per powered family before opening any heldout organism backend evidence.

The geometry freeze records carrier dimension, semantic dimension, chart family, writer family, phase-atlas status, fitting algorithms, thresholds, selection evidence, and hashes. No post-freeze geometry changes are permitted.

## AP14–AP15 — heldout compilation and organism generalization

Only after the AP13 geometry freeze may heldout organisms be opened. The heldout compiler reconstructs the frozen `Q_k` from checkpoint plus the permitted calibration prefix and fits exactly the frozen geometry family. It must not load historical V837an q/backend metadata or V837ao backend metadata, and must not search branch, k, chart family, degree, phase atlas, or writer.

Calibration budgets are exactly `N ∈ {2,4,8,16,32,64}`. Underdetermined budgets are rejected explicitly rather than regularized into pseudo-results. Evaluation is labeled `HELDOUT_ORGANISM / REUSED_HISTORICAL_EPISODE_VALIDATION` and uses the same reader/SET/control/quotient/dynamics/OOD gates. A heldout family pass requires at least 75% of its four holdouts and at least one directed-search plus one random-search organism.

Cross-organism agreement compares canonical semantic values only. It never aligns or directly compares physical STATE40 coordinates or q vectors.

## AP16–AP17 — historical robustness and law audit

Historical validation `20000..20127` is descriptive, post-freeze evidence only. It cannot rescue or upgrade a failed family. The transition-law audit is also non-gating and cannot rescue a family; it asks whether canonical trajectories make the already-known primitive law easy to recover.

## AP18 — diagnosis and closure

AP18 must produce the final decision state, results, permanent failure-memory summary, resource accounting, required plots, report, validators, reproduction entrypoints, and repository status. The diagnosis must follow the evidence actually produced; no family is promoted merely for good decoding.

PrimitiveArchive remains closed in V837ap regardless of outcome. Any next program must be the smallest experiment justified by the frozen V837ap result and must not implicitly start V838.
