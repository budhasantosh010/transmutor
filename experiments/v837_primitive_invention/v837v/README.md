# V837v — Dynamic Control-Scope Localization

V837v isolates **control output scope only**. It starts from V837u `U2_dynamic_scalar_carry`, keeps each source controller strictly local, and varies how many cells consume each source scalar: 10 → 5 → 2 → 1 control domains.

The primary experiment never averages local gates and never introduces a global observer. If V837v does not restore the frozen `>=4/5` representation-adequacy gate, V837w is allowed to interrogate the successful T2 GRU controller's information source.

Fresh-audit seeds remain unused; structural search, primitive mining, and V838 remain blocked.
