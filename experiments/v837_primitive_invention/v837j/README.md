# V837j ? Learned-reference calibration

Question: can small standard learned recurrent references solve the unchanged V837 task families under a matched data/training regime?

This is a diagnostic control, not a Transmutor primitive experiment. GRU/residual/vanilla references never enter structural search or the primitive archive. The compatibility probe first reruns the preserved neutral high-capacity blocker baseline; the primary comparison then uses frozen V837 development/validation seed regions with identical episode IDs for all models.

Run `python run_reference_calibration.py` then `python analyze_results.py`. Fresh-audit seeds remain forbidden.
