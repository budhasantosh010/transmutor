# V837u — Authorized Neutral Dynamic Follow-up

V837t machine-selected `DYNAMIC_SCALAR_CARRY` because the successful GRU retained 4/5 families when its update gate was post-sigmoid scalarized and broadcast.

V837u therefore transfers only adaptive preserve-vs-replace state carry into the historical local 10×4 neutral substrate. It does not add vector control, global coupling, shared state, more data, more steps, search, mining, fresh audit data, or V838.

The exact V837p scalar controller is reused. The causal control keeps the same controller but removes old-state carry via `s_next = g*candidate`.
