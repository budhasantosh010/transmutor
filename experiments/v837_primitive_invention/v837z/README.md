# V837z — Candidate Stage Organization

V837z is machine-authorized only by the failed V837y decision state. It freezes the selected V837y Y3 parent (joint global scalar carry + rank-4 cross-block candidate coupling) and changes exactly one variable: historical mixed same-step message execution versus a fully synchronous previous-output-only candidate stage.

Z0 reproduces the selected parent. Z1 keeps all 55 graph edges, parameters, controller, candidate coupling, state layout, input transforms, optimizer, data, and training budget unchanged; only message source timing changes.
