# V837aq Research Specification

Program: `V837aq_PROGRAM_LEVEL_CAUSAL_OPERATOR_LOCALIZATION`

START SHA: `213f4f06ecaf4d5c767e06389ba5b6e6484a3122`

Branch: `research/v837-program-level-causal-operator-localization`

## Mission

Determine whether independently trained competent AF1D organisms implement the same finite-horizon causal transformation from legitimate semantic interventions to future consequences even when no shared hidden-state coordinate is causally closed.

The invariant under test is an operator/program contract, not a neural coordinate. Every accepted organism must match the exact synthetic-task oracle before cross-organism similarity can count as evidence.

## Gates

AQ2 is a kill gate. The iterative-state harness must recover the exact impulse law `0.35 * 0.65^(h-1) * delta` across multiple timings, magnitudes, horizons, organisms, and both search engines. Failure yields `PROGRAM_OPERATOR_MEASUREMENT_INVALID` and stops broad operator claims.

AQ3 measures first-order response tensors. AQ4 adds only second-order interaction residuals when the oracle itself is materially non-additive; third order is forbidden. AQ5 requires oracle-relative accuracy, cross-organism agreement, and controls including historical incompetents where family-compatible.

AQ6/AQ7 are allowed only after operator identity. They patch naturally occurring outputs/messages/global terms/gates from paired legitimate counterfactual trajectories and localize support in component x time. No arbitrary hidden-state SET is primary evidence.

AQ8 tests composition laws. AQ9 is a fallback predictive-response/Hankel diagnostic, not an automatic escalation. AQ10 uses frozen contracts on held-out organisms with no operator refit. AQ11 decides among the predeclared program-level outcomes.

Fresh audit 90000-90499 remains unused. PrimitiveArchive remains empty. Primitives promoted remain zero. V838 remains unstarted.
