# V837ao — Latent Primitive Canonicalization

V837ao starts from V837an `GENERAL_CAUSAL_LATENT_PRIMITIVE_PATTERN` and tests whether the four validated `STATE40-K1` abstractions are true canonical semantic state variables rather than local steering directions.

The program never aligns 40D microstates or q-vectors across organisms. It defines a canonical IR `P=(Z,U,F,G,Φ)` and independently compiles organism-specific backends `B_i=(E_i,W_i)`. The principal new generalization axis is a deterministic held-out-organism backend split.

Fresh audit seeds remain unused; V838 is not started; primitive promotion remains zero.
